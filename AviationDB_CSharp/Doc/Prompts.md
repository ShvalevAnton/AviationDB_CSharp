# 4
	По примеру таблицы рейсы добавь таблицу Места. Чтобы были методы CRUD и Поиск и Просмотр с помощью пагинации. 
	Места где изменяешь код добавляй комментарий "Изменен". Измени SeatService.cs по примеру FlightService.cs экземпляр DbContext 
	используется в разных потоках или выполняется несколько асинхронных операций одновременно.
	Краткое описание файлов:
	- SeatViewWindow.xaml.cs - код для окна просмотра мест
	- SeatEditWindow.xaml - XAML для окна редактирования/создания мест
	- SeatEditWindow.xaml.cs - код для окна редактирования/создания мест
	- SeatsPage.xaml - страница со списком мест
	- SeatsPage.xaml.cs - код страницы мест
	- SeatsViewModel.cs - ViewModel для страницы мест
	- SeatService.cs - сервис с методами CRUD и пагинации мест

# 3
	Для таблицы seats по примеру предыдущих таблиц :
	- создай модель и Service-класс. 
	- Создать класс меню (наследник MenuService).
	- Добавить в DI контейнер. 
	- Добавить пункт в MainMenuService
	- если данные хранятся в json создай класс конвертер
	- если есть время оно должно быть в формате UTC для перевода из локального времени в UTC используй конвертер DateTimeConverter

	Если существующий код программы редактируется пиши рядом "// ← Добавлено"
	Комментируй код.

# 2	
	Для таблицы boarding_passes по примеру таблицы airports_data :
	- создай модель и Service-класс. 
	- Создать класс меню (наследник MenuService).
	- Добавить в DI контейнер. 
	- Добавить пункт в MainMenuService
	- если данные хранятся в json создай класс конвертер

	Если существующий код программы редактируется пиши рядом "// ← Добавлено"
	Комментируй код.

# 1
		Создай консольную простую программу на C# подключения и редактирование (методы CRUD) базы данных к PostgresSQL. Создай модели БД с 
	методами CRUD. При старте программы появляется меню, где можно выбрать таблицу. После выбора таблицы появляется меню с пунктами CRUD 
	метадами. 
 
	Для подключения к БД используй технологию EF.Core. Покажу структуру проекта. 
	Код пиши с комментариями. 

	Установленные пакеты:
	<PackageReference Include="Microsoft.EntityFrameworkCore.Design" Version="10.0.0">
	<PackageReference Include="Microsoft.Extensions.Configuration.Json" Version="10.0.0" />
	<PackageReference Include="Microsoft.Extensions.DependencyInjection" Version="10.0.0" />
	<PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" Version="10.0.0" />

	Параметры подключения:
	Хост: 127.0.0.1
	Порт: 5432
	База данных: demo
	Пользователь: anton
	Пароль: q1

	Создай первую таблицу aircrafts_data	
	


