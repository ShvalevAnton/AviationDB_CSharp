<Window x:Class="AviationDB_CSharp.Views.BookingViewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Просмотр бронирования" 
        Height="400" 
        Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                   Text="Детальная информация о бронировании"
                   FontSize="16" FontWeight="Bold" Margin="0,0,0,20"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Номер брони:" 
                   FontWeight="Bold" Margin="0,0,10,15"/>
        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding BookRef}" 
                   FontSize="14" FontWeight="Bold" Foreground="#2196F3"
                   Margin="0,0,0,15"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Дата бронирования:" 
                   FontWeight="Bold" Margin="0,0,10,10"/>
        <StackPanel Grid.Row="2" Grid.Column="1" Margin="0,0,0,10">
            <TextBlock Text="{Binding FormattedDate}" FontWeight="Bold"/>
            <TextBlock Text="{Binding FormattedUtcDate}" 
                       FontSize="11" Foreground="Gray" Margin="0,2,0,0"/>
        </StackPanel>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Общая сумма:" 
                   FontWeight="Bold" Margin="0,0,10,10"/>
        <TextBlock Grid.Row="3" Grid.Column="1" 
                   Text="{Binding TotalAmount, StringFormat={}{0:N2} ₽}"
                   FontSize="14" FontWeight="Bold" Foreground="#4CAF50"
                   Margin="0,0,0,10"/>

        
        <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Редактировать" 
                    Click="EditButton_Click"
                    Width="120" Height="30" Margin="0,0,10,0"
                    Background="#2196F3"
                    Foreground="White"
                    FontWeight="Bold"/>
            <Button Content="Закрыть" 
                    Click="CloseButton_Click"
                    Width="100" Height="30"/>
        </StackPanel>
    </Grid>
</Window>