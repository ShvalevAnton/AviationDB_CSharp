<Window x:Class="AviationDB_CSharp.Views.AircraftEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AviationDB_CSharp.Views"
        mc:Ignorable="d"
        Title="Редактирование самолета" Height="350" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Код самолета -->
        <Label Grid.Row="0" Grid.Column="0" Content="Код:" 
               VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="TxtCode"
                 MaxLength="3" IsEnabled="{Binding IsNew}"
                 Margin="0,0,0,10" Height="25"
                 ToolTip="3-символьный код самолета (например: A32, 320, SU9)"/>

        <!-- Модель (EN) -->
        <Label Grid.Row="1" Grid.Column="0" Content="Модель (EN):" 
               VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="TxtModelEn"
                 Margin="0,0,0,10" Height="25"/>

        <!-- Модель (RU) -->
        <Label Grid.Row="2" Grid.Column="0" Content="Модель (RU):" 
               VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="TxtModelRu"
                 Margin="0,0,0,10" Height="25"/>

        <!-- Дальность -->
        <Label Grid.Row="3" Grid.Column="0" Content="Дальность (км):" 
               VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="TxtRange"
                 Margin="0,0,0,10" Height="25"/>

        <!-- Сообщения об ошибках -->
        <TextBlock Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"
                   x:Name="TxtError" Foreground="Red" 
                   TextWrapping="Wrap" Margin="0,10,0,10"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Сохранить" x:Name="BtnSave"
                    Click="SaveButton_Click" Width="80" Margin="0,0,10,0"/>
            <Button Content="Отмена" Click="CancelButton_Click"
                    Width="80"/>
        </StackPanel>
    </Grid>
</Window>
